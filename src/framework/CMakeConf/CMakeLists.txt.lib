cmake_minimum_required(VERSION 2.8.9)
project (libJetScape)

include_directories(./)
file(GLOB SOURCES "./*.cc")

#maybe not all necessary
#add_definitions(-O -fPIC -pipe -Wall -g -std=c++11 -stdlib=libc++ -pthread)
add_definitions(-O -Wall -g -std=c++11 -stdlib=libc++ -pthread)

add_library(JetScape SHARED ${SOURCES})
#add_library(JetScape STATIC ${SOURCES})

#Set the location for library installation -- i.e., /usr/lib in this case
# not really necessary in this example. Use "sudo make install" to apply
#install(TARGETS testStudent DESTINATION /usr/lib)

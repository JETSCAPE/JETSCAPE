At runtime, iS3D selects the pdg file (which contains resonance masses, PIDs, etc...) based on the parameter hrg_eos.

before running iS3D, one also needs to copy the appropriate list of chosen resonances to chosen_particles.dat


hrg_eos = 1 : copy chosen_particles_urqmd.dat (or a subset) to chosen_particles.dat 

hrg_eos = 2 : copy chosen_particles_smash.dat (or a subset) to chosen_particles.dat 

hrg_eos = 3 : copy chosen_particles_box.dat (or a subset) to chosen_particles.dat 

